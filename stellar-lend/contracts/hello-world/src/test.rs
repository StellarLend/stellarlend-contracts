// stellar-lend/contracts/hello-world/src/test.rs
#![allow(clippy::all)]

#[cfg(test)]
mod test {
    #[test]
    fn it_compiles() {
        // Minimal smoke test: ensures the module exists and compiles.
        assert_eq!(2 + 2, 4);
    }
}
